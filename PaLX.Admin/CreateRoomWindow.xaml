<Window x:Class="PaLX.Admin.CreateRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Créer un Salon" Height="550" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    
    <Border Background="White" Margin="20" CornerRadius="10" Padding="20">
        <Border.Effect>
            <DropShadowEffect Color="#bdc3c7" BlurRadius="10" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>
        
        <StackPanel>
            <TextBlock Text="Nouveau Salon" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- Name -->
            <TextBlock Text="Nom du salon" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="RoomNameBox" Padding="5" Margin="0,0,0,15"/>

            <!-- Description -->
            <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="DescriptionBox" Padding="5" Margin="0,0,0,15" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

            <!-- Category -->
            <TextBlock Text="Catégorie" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="CategoryCombo" Margin="0,0,0,15" DisplayMemberPath="Name"/>

            <!-- Subscription Level -->
            <TextBlock Text="Niveau du salon" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="LevelCombo" Margin="0,0,0,15" SelectionChanged="LevelCombo_SelectionChanged">
                <ComboBoxItem Content="Basic (Max 20)" Tag="0"/>
                <ComboBoxItem Content="Deluxe (Max 50)" Tag="1"/>
                <ComboBoxItem Content="VIP (Max 100)" Tag="2"/>
                <ComboBoxItem Content="Royal (Max 200)" Tag="3"/>
                <ComboBoxItem Content="Legend (Max 500)" Tag="4"/>
            </ComboBox>

            <!-- Options -->
            <CheckBox x:Name="PrivateCheck" Content="Salon Privé (Mot de passe requis)" Margin="0,0,0,10" Checked="PrivateCheck_Changed" Unchecked="PrivateCheck_Changed"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,15" Padding="5" Visibility="Collapsed"/>

            <CheckBox x:Name="AdultCheck" Content="Contenu Adulte (18+)" Margin="0,0,0,20"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Annuler" Width="100" Height="35" Margin="0,0,10,0" Click="Cancel_Click" Background="#95a5a6" Foreground="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="Créer" Width="100" Height="35" Click="Create_Click" Background="{StaticResource PrimaryBrush}" Foreground="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>