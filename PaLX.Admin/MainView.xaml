<Window x:Class="PaLX.Admin.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PaL.Xtreme Admin" Height="700" Width="380"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f0f0f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu -->
            <RowDefinition Height="Auto"/> <!-- User Info -->
            <RowDefinition Height="*"/>    <!-- Friends List -->
            <RowDefinition Height="Auto"/> <!-- Bottom Bar -->
        </Grid.RowDefinitions>

        <!-- Menu -->
        <Menu Grid.Row="0" Background="White" Padding="5">
            <MenuItem Header="Fichier">
                <MenuItem Header="Quitter" Click="Logout_Click"/>
            </MenuItem>
            <MenuItem Header="Aide">
                <MenuItem Header="À propos"/>
            </MenuItem>
        </Menu>

        <!-- User Info -->
        <Border Grid.Row="1" Background="White" Padding="15" Margin="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect Color="#bdc3c7" BlurRadius="5" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Avatar -->
                <Ellipse Width="60" Height="60" Stroke="{StaticResource PrimaryBrush}" StrokeThickness="2">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="#ecf0f1"/>
                    </Ellipse.Fill>
                    <Ellipse.ToolTip>
                        <ToolTip Content="Votre Avatar"/>
                    </Ellipse.ToolTip>
                </Ellipse>
                <TextBlock Text="🛡️" FontSize="30" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False"/>

                <!-- Info -->
                <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <Ellipse Width="12" Height="12" Fill="{Binding SelectedItem.ColorBrush, ElementName=StatusCombo}" Margin="0,0,8,0"/>
                        <TextBlock x:Name="UsernameText" Text="Username" FontWeight="Bold" FontSize="18" Foreground="{StaticResource TextBrush}"/>
                    </StackPanel>
                    
                    <ComboBox x:Name="StatusCombo" SelectedIndex="0" BorderThickness="0" Background="White" Width="150" HorizontalAlignment="Left">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="{Binding ColorBrush}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding Name}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Friends List -->
        <ListView Grid.Row="2" x:Name="FriendsList" BorderThickness="0" Background="Transparent" Margin="0,10" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" CornerRadius="8" Margin="5,2" Padding="10" Width="330">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse Width="40" Height="40" Margin="0,0,15,0" Fill="#ecf0f1"/>
                            <TextBlock Text="👤" FontSize="20" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False"/>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource TextBrush}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{StaticResource LightTextBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Bottom Bar -->
        <Border Grid.Row="3" Background="White" Padding="10" BorderBrush="#ecf0f1" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Settings -->
                <Button Grid.Column="0" Style="{StaticResource IconButtonStyle}" ToolTip="Paramètres">
                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{StaticResource TextBrush}"/>
                </Button>

                <!-- Add Friend -->
                <Button Grid.Column="1" Style="{StaticResource IconButtonStyle}" ToolTip="Ajouter un ami">
                    <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="{StaticResource PrimaryBrush}"/>
                </Button>

                <!-- Logout -->
                <Button Grid.Column="2" Style="{StaticResource IconButtonStyle}" Click="Logout_Click" ToolTip="Se déconnecter">
                    <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{StaticResource TextBrush}"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>