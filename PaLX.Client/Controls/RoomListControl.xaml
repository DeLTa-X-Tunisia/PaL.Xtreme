<UserControl x:Class="PaLX.Client.Controls.RoomListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="350">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="RoomItemStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#bdc3c7" BlurRadius="3" ShadowDepth="1" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header / Filter -->
            <RowDefinition Height="*"/>    <!-- List -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal">
                <ComboBox x:Name="CategoryFilter" SelectionChanged="CategoryFilter_SelectionChanged" Width="150" HorizontalAlignment="Left">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock Text="v2.0" Foreground="Red" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" Content="+ Créer" Click="CreateRoom_Click" 
                    Background="{StaticResource PrimaryBrush}" Foreground="White" 
                    Padding="10,5" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <!-- Room List -->
        <ListView Grid.Row="1" x:Name="RoomsList" BorderThickness="0" Background="Transparent" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" MouseDoubleClick="RoomsList_MouseDoubleClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource RoomItemStyle}" Opacity="{Binding Opacity}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/> <!-- Icon/Level -->
                                <ColumnDefinition Width="*"/>    <!-- Info -->
                                <ColumnDefinition Width="Auto"/> <!-- Users/Status -->
                            </Grid.ColumnDefinitions>

                            <!-- Level Icon -->
                            <Border Width="40" Height="40" CornerRadius="20" Background="{Binding LevelColor}" Margin="0,0,10,0">
                                <TextBlock Text="{Binding LevelInitial}" Foreground="White" FontSize="18" FontWeight="Bold" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <!-- Info -->
                            <Grid Grid.Column="1" VerticalAlignment="Center">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="#2c3e50" TextTrimming="CharacterEllipsis"/>
                                    <!-- Badges -->
                                    <Border Background="#e74c3c" CornerRadius="3" Margin="5,0,0,0" Padding="3,0" Visibility="{Binding Is18Plus, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="18+" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                    </Border>
                                    <Border Background="#f1c40f" CornerRadius="3" Margin="3,0,0,0" Padding="3,0" Visibility="{Binding IsVIP, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="VIP" Foreground="Black" FontSize="10" FontWeight="Bold"/>
                                    </Border>
                                    <Border Background="#95a5a6" CornerRadius="3" Margin="3,0,0,0" Padding="3,0" Visibility="{Binding IsPrivate, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="🔒" Foreground="White" FontSize="10"/>
                                    </Border>
                                </StackPanel>
                                <TextBlock Grid.Row="1" Text="{Binding CategoryName}" FontSize="11" Foreground="Gray"/>
                                <TextBlock Grid.Row="2" Text="{Binding OwnerName, StringFormat='Par : {0}'}" FontSize="11" Foreground="#34495e"/>
                                <TextBlock Grid.Row="3" Text="{Binding Description}" FontSize="12" Foreground="#7f8c8d" TextTrimming="CharacterEllipsis"/>
                            </Grid>

                            <!-- Users Count & Actions -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="👥" FontSize="12" Foreground="Gray" Margin="0,0,3,0"/>
                                    <TextBlock Text="{Binding UserCount}" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="/" FontSize="12" Foreground="Gray"/>
                                    <TextBlock Text="{Binding MaxUsers}" FontSize="12" Foreground="Gray"/>
                                </StackPanel>

                                <!-- Edit Button: Visible for Owner OR Admin/Moderator -->
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0" Visibility="{Binding CanEdit, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Button Content="✏️" ToolTip="Modifier" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="EditRoom_Click" Tag="{Binding}" FontSize="14"/>
                                </StackPanel>
                                
                                <!-- Owner & System Admin Actions: Hide/Show & Delete -->
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0" Visibility="{Binding HasOwnerAccess, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Button Content="{Binding VisibilityIcon}" ToolTip="{Binding VisibilityTooltip}" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="HideRoom_Click" Tag="{Binding}" FontSize="14"/>
                                    <Button Content="🗑️" ToolTip="Supprimer" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="DeleteRoom_Click" Tag="{Binding}" Foreground="Red" Margin="5,0,0,0" FontSize="14"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
