<Window x:Class="PaLX.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaLX.Client"
        mc:Ignorable="d"
        Title="PaL.Xtreme" Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Grid>
        <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="10" Margin="50">
            <Border.Effect>
                <DropShadowEffect Color="#bdc3c7" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
            </Border.Effect>
            <Grid>
                <local:LoginView x:Name="LoginView" Visibility="Visible"/>
                <local:RegisterView x:Name="RegisterView" Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</Window>
