<Window x:Class="PaLX.Client.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:PaLX.Client.Controls"
        mc:Ignorable="d"
        Title="PaL.Xtreme" Height="850" Width="480"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        MinHeight="600" MinWidth="400">
        
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Modern Tab Style -->
        <Style x:Key="ModernTabStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="Transparent" Padding="20,12" Cursor="Hand" CornerRadius="12">
                            <ContentPresenter x:Name="ContentSite"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#15E03E2F"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="#08000000"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Friend Card Style -->
        <Style x:Key="FriendCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Modern Nav Button Style -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="52"/>
            <Setter Property="Height" Value="52"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="16" Width="48" Height="48">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <!-- Active indicator line -->
                            <Border x:Name="indicator" Height="3" Width="20" Background="{StaticResource PrimaryBrush}" 
                                    CornerRadius="2" VerticalAlignment="Bottom" Margin="0,0,0,-6" Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Window Control Button -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#10000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseWindowButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="0,20,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Local IconButtonStyle (override from App.xaml for list items) -->
        <Style x:Key="LocalIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- Main Container with Shadow -->
    <Border CornerRadius="20" Background="{DynamicResource BackgroundBrush}" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="0" Opacity="0.15"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- User Profile Card -->
                <RowDefinition Height="Auto"/> <!-- Tab Headers -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Bottom Nav -->
            </Grid.RowDefinitions>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- HEADER WITH WINDOW CONTROLS                                      -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="20,20,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid Height="50">
                    <!-- App Title -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="🔥" FontSize="20" VerticalAlignment="Center"/>
                        <TextBlock Text="PaL.Xtreme" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- Window Controls -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource WindowControlButton}" Click="MinimizeButton_Click">
                            <TextBlock Text="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Button>
                        <Button Style="{StaticResource WindowControlButton}" Click="MaximizeButton_Click">
                            <TextBlock x:Name="MaximizeIcon" Text="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Button>
                        <Button Style="{StaticResource CloseWindowButton}" Click="CloseButton_Click">
                            <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- USER PROFILE CARD                                                -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" Margin="16,8,16,8" CornerRadius="20" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="2" Opacity="0.06"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Avatar -->
                    <Grid>
                        <Ellipse x:Name="UserAvatar" Width="64" Height="64" Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="3">
                            <Ellipse.Fill>
                                <SolidColorBrush Color="{DynamicResource BorderColor}"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <TextBlock x:Name="AvatarPlaceholder" Text="👤" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>
                    </Grid>

                    <!-- User Info -->
                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="UsernameText" Text="Username" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource TextBrush}"/>
                        
                        <!-- Status Selector -->
                        <ComboBox x:Name="StatusCombo" SelectedIndex="0" Margin="0,6,0,0" Width="160" HorizontalAlignment="Left">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Width="10" Height="10" Fill="{Binding ColorBrush}" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding Name}" FontSize="13"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>
                    
                    <!-- Edit Profile Button -->
                    <Button Grid.Column="2" Style="{StaticResource CircleIconButtonStyle}" ToolTip="Modifier le profil" Click="Profile_Click">
                        <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Button>
                </Grid>
            </Border>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- TAB HEADERS                                                      -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="2" Background="{DynamicResource CardBackgroundBrush}" Margin="16,0,16,8" CornerRadius="16" Padding="4">
                <TabControl x:Name="MainTabControl" BorderThickness="0" Background="Transparent" Padding="0" SelectionChanged="MainTabControl_SelectionChanged">
                    <TabControl.Template>
                        <ControlTemplate TargetType="TabControl">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="0"/>
                                </Grid.RowDefinitions>
                                <TabPanel Grid.Row="0" IsItemsHost="True" HorizontalAlignment="Center" Margin="4"/>
                            </Grid>
                        </ControlTemplate>
                    </TabControl.Template>

                    <!-- Tab: Amis -->
                    <TabItem Header="👥 Amis" Style="{StaticResource ModernTabStyle}"/>
                    
                    <!-- Tab: Discussions -->
                    <TabItem Style="{StaticResource ModernTabStyle}">
                        <TabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💬 Discussions"/>
                                <Border Background="{StaticResource PrimaryBrush}" CornerRadius="10" MinWidth="20" Height="20" Margin="8,0,0,0" Padding="6,0" 
                                        Visibility="{Binding TotalUnreadVisibility, RelativeSource={RelativeSource AncestorType=Window}}">
                                    <TextBlock Text="{Binding TotalUnreadCount, RelativeSource={RelativeSource AncestorType=Window}}" 
                                               Foreground="White" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </TabItem.Header>
                    </TabItem>
                    
                    <!-- Tab: Salons -->
                    <TabItem x:Name="RoomsTab" Header="🏠 Salons" Style="{StaticResource ModernTabStyle}" Visibility="Visible"/>
                </TabControl>
            </Border>
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- CONTENT AREA - FRIENDS LIST                                          -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="3" Margin="16,0,16,0">
                <!-- Friends List (Tab 0) -->
                <ListView x:Name="FriendsList" BorderThickness="0" Background="Transparent" 
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource FriendCardStyle}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Avatar with Status Indicator -->
                                    <Grid Margin="0,0,12,0">
                                        <!-- Avatar -->
                                        <Ellipse Width="50" Height="50" Stroke="{StaticResource BorderBrush}" StrokeThickness="2" Visibility="{Binding AvatarVisibility}">
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                   IsHitTestVisible="False" Visibility="{Binding PlaceholderVisibility}"/>
                                        
                                        <!-- Block Overlay -->
                                        <Ellipse Width="50" Height="50" Fill="#80FFFFFF" Visibility="{Binding BlockOverlayVisibility}"/>
                                        <TextBlock Text="🚫" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                   Visibility="{Binding BlockOverlayVisibility}"/>
                                        
                                        <!-- Online Status Dot -->
                                        <Ellipse Width="14" Height="14" Fill="{Binding StatusColor}" 
                                                 Stroke="White" StrokeThickness="2"
                                                 HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,2,2"/>
                                    </Grid>
                                    
                                    <!-- Name & Status -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="FontWeight" Value="{Binding NameFontWeight}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsBlinking}" Value="True">
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard x:Name="BlinkStoryboard">
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                         From="1.0" To="0.3" Duration="0:0:0.5"
                                                                                         AutoReverse="True" RepeatBehavior="Forever"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <DataTrigger.ExitActions>
                                                                <StopStoryboard BeginStoryboardName="BlinkStoryboard"/>
                                                            </DataTrigger.ExitActions>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{Binding StatusText}" FontSize="13" Foreground="{Binding StatusColor}" Margin="0,3,0,0"/>
                                    </StackPanel>

                                    <!-- Action Buttons -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Style="{StaticResource LocalIconButtonStyle}" ToolTip="Discuter" Click="OpenChat_Click" Tag="{Binding Username}">
                                            <TextBlock Text="&#xE8BD;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource PrimaryBrush}"/>
                                        </Button>
                                        <Button Style="{StaticResource LocalIconButtonStyle}" ToolTip="Profil" Click="ViewProfile_Click" Tag="{Binding Username}">
                                            <TextBlock Text="&#xE779;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </Button>
                                        <Button Style="{StaticResource LocalIconButtonStyle}" ToolTip="{Binding BlockToolTip}" Click="BlockUser_Click" Tag="{Binding Username}">
                                            <TextBlock Text="{Binding BlockIcon}" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="{StaticResource DangerBrush}"/>
                                        </Button>
                                        <Button Style="{StaticResource LocalIconButtonStyle}" ToolTip="Retirer" Click="RemoveFriend_Click" Tag="{Binding Username}">
                                            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#e74c3c"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Conversations List (Tab 1) -->
                <ListView x:Name="ConversationsList" BorderThickness="0" Background="Transparent" 
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                          MouseDoubleClick="ConversationsList_MouseDoubleClick" Visibility="Collapsed">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource FriendCardStyle}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.04"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Avatar -->
                                    <Grid Margin="0,0,12,0">
                                        <Ellipse Width="50" Height="50" Stroke="{StaticResource BorderBrush}" StrokeThickness="2">
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AvatarPath}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock Text="👤" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AvatarPath}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>

                                    <!-- Content -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource TextBrush}"/>
                                            <!-- Unknown Sender Indicator -->
                                            <Border Background="{StaticResource DangerBrush}" CornerRadius="6" Margin="8,0,0,0" Padding="6,2" Visibility="{Binding UnknownSenderVisibility}">
                                                <TextBlock Text="Inconnu" Foreground="White" FontSize="10" FontWeight="SemiBold"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding LastMessage}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" 
                                                   TextTrimming="CharacterEllipsis" MaxWidth="220" Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <!-- Time & Badge -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding TimeDisplay}" FontSize="11" Foreground="{StaticResource LightTextBrush}" HorizontalAlignment="Right"/>
                                        <Border Background="{StaticResource PrimaryBrush}" CornerRadius="12" MinWidth="24" Height="24" 
                                                Visibility="{Binding UnreadVisibility}" Margin="0,6,0,0" HorizontalAlignment="Right">
                                            <TextBlock Text="{Binding UnreadCount}" Foreground="White" FontSize="11" FontWeight="Bold" 
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="8,0"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Rooms Control (Tab 2) -->
                <controls:RoomListControl x:Name="RoomListControl" Visibility="Collapsed"/>
            </Grid>

            <!-- ═══════════════════════════════════════════════════════════════ -->
            <!-- MODERN BOTTOM NAVIGATION BAR                                     -->
            <!-- ═══════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="4" Background="{DynamicResource CardBackgroundBrush}" CornerRadius="0,0,20,20" Padding="12,8,12,12">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="-4" Opacity="0.08" Direction="90"/>
                </Border.Effect>
                
                <!-- Floating Nav Container -->
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="20" Padding="8,4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/> <!-- Center action button -->
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Settings -->
                        <Button Grid.Column="0" x:Name="SettingsButton" Style="{StaticResource NavButtonStyle}" ToolTip="Paramètres" Click="SettingsButton_Click">
                            <Border Background="{DynamicResource BorderBrush}" CornerRadius="12" Width="40" Height="40">
                                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="18" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <ContextMenu.Resources>
                                        <Style TargetType="MenuItem">
                                            <Setter Property="Padding" Value="12,10"/>
                                            <Setter Property="FontSize" Value="13"/>
                                        </Style>
                                        <Style TargetType="Separator">
                                            <Setter Property="Margin" Value="8,4"/>
                                        </Style>
                                    </ContextMenu.Resources>
                                    
                                    <!-- Profile -->
                                    <MenuItem Click="Profile_Click">
                                        <MenuItem.Header>
                                            <StackPanel Orientation="Horizontal">
                                                <Border Background="#E3F2FD" CornerRadius="6" Width="28" Height="28" Margin="0,0,10,0">
                                                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="13" 
                                                               Foreground="#1976D2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="Mon Profil" FontWeight="SemiBold"/>
                                                    <TextBlock Text="Modifier mes informations" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </MenuItem.Header>
                                    </MenuItem>
                                    
                                    <!-- Settings -->
                                    <MenuItem Click="Settings_Click">
                                        <MenuItem.Header>
                                            <StackPanel Orientation="Horizontal">
                                                <Border Background="#F3E5F5" CornerRadius="6" Width="28" Height="28" Margin="0,0,10,0">
                                                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="13" 
                                                               Foreground="#7B1FA2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="Paramètres" FontWeight="SemiBold"/>
                                                    <TextBlock Text="Thème, sons, préférences" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </MenuItem.Header>
                                    </MenuItem>
                                    
                                    <Separator/>
                                    
                                    <!-- Blocked Users -->
                                    <MenuItem Click="BlockedUsers_Click">
                                        <MenuItem.Header>
                                            <StackPanel Orientation="Horizontal">
                                                <Border Background="#FFEBEE" CornerRadius="6" Width="28" Height="28" Margin="0,0,10,0">
                                                    <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="13" 
                                                               Foreground="#C62828" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel VerticalAlignment="Center">
                                                    <TextBlock Text="Utilisateurs bloqués" FontWeight="SemiBold"/>
                                                    <TextBlock Text="Gérer la liste noire" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </MenuItem.Header>
                                    </MenuItem>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>

                        <!-- Friend Requests -->
                        <Button Grid.Column="1" x:Name="NotificationButton" Click="NotificationButton_Click" Style="{StaticResource NavButtonStyle}" ToolTip="Demandes d'amis">
                            <Grid>
                                <Border Background="{DynamicResource BorderBrush}" CornerRadius="12" Width="40" Height="40">
                                    <TextBlock Text="&#xE716;" FontFamily="Segoe MDL2 Assets" FontSize="18" 
                                               Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <Border Background="{StaticResource PrimaryBrush}" CornerRadius="10" MinWidth="20" Height="20" 
                                        HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-4,-4,0" Padding="4,0"
                                        Visibility="{Binding HasNotifications, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="{Binding NotificationCount}" Foreground="White" FontSize="10" FontWeight="Bold" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Grid>
                        </Button>

                        <!-- Central Add Friend Button (Highlighted) -->
                        <Button Grid.Column="2" Style="{StaticResource NavButtonStyle}" ToolTip="Ajouter un ami" Click="AddFriend_Click" 
                                Width="60" Height="60" Margin="8,0">
                            <Border Background="{StaticResource PrimaryBrush}" CornerRadius="18" Width="52" Height="52">
                                <Border.Effect>
                                    <DropShadowEffect Color="{StaticResource PrimaryColor}" BlurRadius="16" ShadowDepth="2" Opacity="0.4"/>
                                </Border.Effect>
                                <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="22" 
                                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Button>

                        <!-- Rooms -->
                        <Button Grid.Column="3" x:Name="RoomsButton" Style="{StaticResource NavButtonStyle}" ToolTip="Salons" Click="RoomsButton_Click" Visibility="Visible">
                            <Border Background="{DynamicResource BorderBrush}" CornerRadius="12" Width="40" Height="40">
                                <TextBlock Text="&#xE909;" FontFamily="Segoe MDL2 Assets" FontSize="18" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Button>

                        <!-- Logout -->
                        <Button Grid.Column="4" Style="{StaticResource NavButtonStyle}" Click="Logout_Click" ToolTip="Se déconnecter">
                            <Border Background="{DynamicResource BorderBrush}" CornerRadius="12" Width="40" Height="40">
                                <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" FontSize="18" 
                                           Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Button>
                    </Grid>
                </Border>
            </Border>
        </Grid>
    </Border>
</Window>