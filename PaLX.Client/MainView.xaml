<Window x:Class="PaLX.Client.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PaL.Xtreme Client" Height="700" Width="380"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20" Padding="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#f0f0f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu -->
            <RowDefinition Height="Auto"/> <!-- User Info -->
            <RowDefinition Height="*"/>    <!-- Friends List -->
            <RowDefinition Height="Auto"/> <!-- Bottom Bar -->
        </Grid.RowDefinitions>

        <!-- Menu -->
        <Menu Grid.Row="0" Background="White" Padding="5">
            <MenuItem Header="Fichier">
                <MenuItem Header="Quitter" Click="Logout_Click"/>
            </MenuItem>
            <MenuItem Header="Aide">
                <MenuItem Header="À propos"/>
            </MenuItem>
        </Menu>

        <!-- User Info -->
        <Border Grid.Row="1" Background="White" Padding="15" Margin="0,0,0,1">
            <Border.Effect>
                <DropShadowEffect Color="#bdc3c7" BlurRadius="5" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Avatar -->
                <Ellipse x:Name="UserAvatar" Width="60" Height="60" Stroke="{StaticResource PrimaryBrush}" StrokeThickness="2">
                    <Ellipse.Fill>
                        <SolidColorBrush Color="#ecf0f1"/>
                    </Ellipse.Fill>
                    <Ellipse.ToolTip>
                        <ToolTip Content="Votre Avatar"/>
                    </Ellipse.ToolTip>
                </Ellipse>
                <TextBlock x:Name="AvatarPlaceholder" Text="👤" FontSize="30" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="15,0,0,0" IsHitTestVisible="False"/>

                <!-- Info -->
                <StackPanel Grid.Column="1" Margin="15,0,0,0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock x:Name="UsernameText" Text="Username" FontWeight="Bold" FontSize="18" Foreground="{StaticResource TextBrush}"/>
                    </StackPanel>
                    
                    <ComboBox x:Name="StatusCombo" SelectedIndex="0" BorderThickness="0" Background="White" BorderBrush="White" Width="150" HorizontalAlignment="Left">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="10" Height="10" Fill="{Binding ColorBrush}" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding Name}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="2" Margin="0,10" BorderThickness="0" Background="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="0,0,0,2" BorderBrush="Transparent" Margin="10,0">
                                    <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,5"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter Property="Foreground" Value="Gray"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Tab 1: Amis -->
            <TabItem Header="Amis">
                <ListView x:Name="FriendsList" BorderThickness="0" Background="Transparent" Margin="0,10" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="8" Margin="5,2" Padding="10" Width="330">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Avatar with Block Overlay -->
                                    <Grid Margin="0,0,15,0">
                                        <Ellipse Width="40" Height="40" Stroke="{StaticResource PrimaryBrush}" StrokeThickness="1" Visibility="{Binding AvatarVisibility}">
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock Text="👤" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="{Binding PlaceholderVisibility}"/>
                                        
                                        <!-- Block Overlay -->
                                        <Ellipse Width="40" Height="40" Fill="#99FFFFFF" Visibility="{Binding BlockOverlayVisibility}"/>
                                        <TextBlock Text="🚫" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Red" Visibility="{Binding BlockOverlayVisibility}"/>
                                    </Grid>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="14">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="FontWeight" Value="{Binding NameFontWeight}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsBlinking}" Value="True">
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard x:Name="BlinkStoryboard">
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                         From="1.0" To="0.2" Duration="0:0:0.5"
                                                                                         AutoReverse="True" RepeatBehavior="Forever"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <DataTrigger.ExitActions>
                                                                <StopStoryboard BeginStoryboardName="BlinkStoryboard"/>
                                                            </DataTrigger.ExitActions>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                            <Ellipse Width="8" Height="8" Fill="{Binding StatusColor}" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{Binding StatusColor}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Style="{StaticResource IconButtonStyle}" ToolTip="Discuter" Click="OpenChat_Click" Tag="{Binding Username}">
                                            <TextBlock Text="&#xE8BD;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource PrimaryBrush}"/>
                                        </Button>
                                        <Button Style="{StaticResource IconButtonStyle}" ToolTip="Voir Profil" Click="ViewProfile_Click" Tag="{Binding Username}">
                                            <TextBlock Text="&#xE779;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource TextBrush}"/>
                                        </Button>
                                        <Button Style="{StaticResource IconButtonStyle}" ToolTip="{Binding BlockToolTip}" Click="BlockUser_Click" Tag="{Binding Username}">
                                            <Grid>
                                                <TextBlock Text="{Binding BlockIcon}" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="Red"/>
                                            </Grid>
                                        </Button>
                                        <Button Style="{StaticResource IconButtonStyle}" ToolTip="Retirer" Click="RemoveFriend_Click" Tag="{Binding Username}">
                                            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#e74c3c"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>

            <!-- Tab 2: Discussions -->
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Discussions"/>
                        <Border Background="Red" CornerRadius="8" MinWidth="16" Height="16" Margin="5,0,0,0" Padding="4,0" Visibility="{Binding TotalUnreadVisibility, RelativeSource={RelativeSource AncestorType=Window}}">
                            <TextBlock Text="{Binding TotalUnreadCount, RelativeSource={RelativeSource AncestorType=Window}}" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </TabItem.Header>
                <ListView x:Name="ConversationsList" BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" MouseDoubleClick="ConversationsList_MouseDoubleClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="8" Margin="5,2" Padding="10" Width="330">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Avatar -->
                                    <Grid Margin="0,0,15,0">
                                        <Ellipse Width="40" Height="40" Stroke="{StaticResource PrimaryBrush}" StrokeThickness="1">
                                            <Ellipse.Fill>
                                                <ImageBrush ImageSource="{Binding AvatarPath}" Stretch="UniformToFill"/>
                                            </Ellipse.Fill>
                                            <Ellipse.Style>
                                                <Style TargetType="Ellipse">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AvatarPath}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Ellipse.Style>
                                        </Ellipse>
                                        <TextBlock Text="👤" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding AvatarPath}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>

                                    <!-- Content -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextBrush}"/>
                                            <!-- Unknown Sender Indicator -->
                                            <Border Background="#e74c3c" CornerRadius="4" Margin="5,0,0,0" Padding="4,1" Visibility="{Binding UnknownSenderVisibility}">
                                                <TextBlock Text="Inconnu" Foreground="White" FontSize="10"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding LastMessage}" FontSize="12" Foreground="Gray" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                    </StackPanel>

                                    <!-- Time & Badge -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding TimeDisplay}" FontSize="10" Foreground="Gray" HorizontalAlignment="Right" Margin="0,0,0,5"/>
                                        <Border Background="{StaticResource PrimaryBrush}" CornerRadius="10" MinWidth="20" Height="20" Visibility="{Binding UnreadVisibility}">
                                            <TextBlock Text="{Binding UnreadCount}" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </TabItem>
        </TabControl>

        <!-- Bottom Bar -->
        <Border Grid.Row="3" Background="White" Padding="10" BorderBrush="#ecf0f1" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Settings -->
                <Button Grid.Column="0" x:Name="SettingsButton" Style="{StaticResource IconButtonStyle}" ToolTip="Paramètres" Click="SettingsButton_Click">
                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{StaticResource TextBrush}"/>
                    <Button.ContextMenu>
                        <ContextMenu Placement="Top">
                            <MenuItem Header="Mon Profil" Click="Profile_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Paramètres" Click="Settings_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem Header="Utilisateurs bloqués" Click="BlockedUsers_Click">
                                <MenuItem.Icon>
                                    <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <!-- Notifications -->
                <Button Grid.Column="1" x:Name="NotificationButton" Click="NotificationButton_Click" Style="{StaticResource IconButtonStyle}" ToolTip="Demandes d'amis">
                     <Grid>
                        <TextBlock Text="👥" FontSize="24" Foreground="{StaticResource TextBrush}"/>
                        <Border Background="Red" CornerRadius="10" MinWidth="16" Height="16" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-8,-5,0,0" Padding="4,0" Visibility="{Binding HasNotifications, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="{Binding NotificationCount}" Foreground="White" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                        </Border>
                     </Grid>
                </Button>

                <!-- Add Friend -->
                <Button Grid.Column="2" Style="{StaticResource IconButtonStyle}" ToolTip="Ajouter un ami" Click="AddFriend_Click">
                    <TextBlock Text="&#xE710;" FontFamily="Segoe MDL2 Assets" FontSize="24" Foreground="{StaticResource PrimaryBrush}"/>
                </Button>

                <!-- Logout -->
                <Button Grid.Column="3" Style="{StaticResource IconButtonStyle}" Click="Logout_Click" ToolTip="Se déconnecter">
                    <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="{StaticResource TextBrush}"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>