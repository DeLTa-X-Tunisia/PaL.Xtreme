<Window x:Class="PaLX.Client.UserProfiles"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Mon Profil" Height="580" Width="720"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#E03E2F"/>
                                <Setter Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Height" Value="46"/>
        </Style>

        <!-- Modern DatePicker Style -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Height" Value="46"/>
        </Style>

        <!-- Field Label Style -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#5D6D7E"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E03E2F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C0392B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A93226"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F5F6FA"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Container with Shadow -->
    <Border Background="White" CornerRadius="16" Margin="10" MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="#1A1A2E" BlurRadius="30" ShadowDepth="0" Opacity="0.15"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Avatar & Info -->
            <Border Grid.Column="0" CornerRadius="16,0,0,16">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#E03E2F" Offset="0"/>
                        <GradientStop Color="#C0392B" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <!-- Close Button -->
                    <Button HorizontalAlignment="Left" VerticalAlignment="Top" Margin="12" 
                            Width="32" Height="32" Background="#20FFFFFF" BorderThickness="0" Cursor="Hand"
                            Click="Cancel_Click" ToolTip="Fermer">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8">
                                    <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12" 
                                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bg" Property="Background" Value="#40FFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <!-- Avatar Section -->
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <!-- Avatar Container -->
                        <Grid Width="120" Height="120" Cursor="Hand" MouseLeftButtonDown="Avatar_MouseLeftButtonDown">
                            <Ellipse x:Name="AvatarEllipse" 
                                     Stroke="White" StrokeThickness="4" 
                                     Fill="#40FFFFFF">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            <TextBlock x:Name="AvatarPlaceholder" Text="👤" FontSize="50" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            
                            <!-- Upload Button -->
                            <Button Width="36" Height="36" HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                                    Margin="0,0,4,4" Background="White" BorderThickness="0" Cursor="Hand" 
                                    ToolTip="Changer la photo" Click="UploadAvatar_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="uploadBg" Background="White" CornerRadius="18">
                                            <Border.Effect>
                                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="0" Opacity="0.2"/>
                                            </Border.Effect>
                                            <TextBlock Text="&#xE722;" FontFamily="Segoe MDL2 Assets" 
                                                       Foreground="#E03E2F" FontSize="16" 
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="uploadBg" Property="Background" Value="#FFF5F5"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>

                        <!-- Title -->
                        <TextBlock Text="Mon Profil" FontSize="22" FontWeight="Bold" 
                                   Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,4"/>
                        <TextBlock Text="Personnalisez vos informations" FontSize="12" 
                                   Foreground="#DDFFFFFF" HorizontalAlignment="Center" TextWrapping="Wrap" 
                                   TextAlignment="Center" MaxWidth="180"/>

                        <!-- Decorative element -->
                        <Border Width="40" Height="3" Background="#40FFFFFF" CornerRadius="2" Margin="0,20,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Right Panel - Form -->
            <Grid Grid.Column="1" Margin="30,24,30,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Informations personnelles" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="Les champs marqués * sont obligatoires" FontSize="12" Foreground="#95A5A6" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Form Fields - 2 Columns -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Row 1: Nom & Prénom -->
                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,16">
                        <TextBlock Text="Nom *" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="LastNameBox" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,16">
                        <TextBlock Text="Prénom *" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="FirstNameBox" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <!-- Row 2: Email & Téléphone -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,16">
                        <TextBlock Text="Email *" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="EmailBox" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,16">
                        <TextBlock Text="Téléphone" Style="{StaticResource FieldLabel}"/>
                        <TextBox x:Name="PhoneBox" Style="{StaticResource ModernTextBox}" Tag="+216 XX XX XX XX"/>
                    </StackPanel>

                    <!-- Row 3: Date de naissance & Genre -->
                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,16">
                        <TextBlock Text="Date de naissance *" Style="{StaticResource FieldLabel}"/>
                        <DatePicker x:Name="DobPicker" Style="{StaticResource ModernDatePicker}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,16">
                        <TextBlock Text="Genre" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="GenderCombo" Style="{StaticResource ModernComboBox}" SelectedIndex="0">
                            <ComboBoxItem Content="Homme"/>
                            <ComboBoxItem Content="Femme"/>
                            <ComboBoxItem Content="Autre"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Row 4: Pays (full width) -->
                    <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3">
                        <TextBlock Text="Pays *" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="CountryCombo" Style="{StaticResource ModernComboBox}" IsEditable="True"/>
                    </StackPanel>
                </Grid>

                <!-- Footer Buttons -->
                <Grid Grid.Row="2" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="Annuler" Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
                    <Button Grid.Column="2" Click="Done_Click" Style="{StaticResource PrimaryButton}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Enregistrer" VerticalAlignment="Center"/>
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="14" 
                                       Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>

            <!-- Validation Popup Overlay -->
            <Grid x:Name="ValidationPopup" Grid.ColumnSpan="2" Visibility="Collapsed" Background="#7F000000">
                <Border Background="White" CornerRadius="16" HorizontalAlignment="Center" VerticalAlignment="Center" 
                        Width="320" Padding="28">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <Grid Width="60" Height="60" Margin="0,0,0,16">
                            <Ellipse Fill="#FFF3E0"/>
                            <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="28" 
                                       Foreground="#F39C12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="Profil Incomplet" FontWeight="Bold" FontSize="18" 
                                   HorizontalAlignment="Center" Margin="0,0,0,8" Foreground="#2C3E50"/>
                        <TextBlock x:Name="ValidationMessage" Text="Message here..." TextWrapping="Wrap" 
                                   TextAlignment="Center" Foreground="#7F8C8D" FontSize="14" Margin="0,0,0,24"/>
                        <Button Content="OK, je complète" Click="ClosePopup_Click" Style="{StaticResource PrimaryButton}" 
                                HorizontalAlignment="Stretch"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>